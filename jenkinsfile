pipeline{
          agent any
          stages{
                 stage(build){
                               steps{
                                     echo 'jenkins automation project'
                                     sh './gradlew build --no-daemon'
                                     archiveArtifacts artifacts: 'dist/train-schedule.zip'                                    
                                    }
                             }
               }
        }
