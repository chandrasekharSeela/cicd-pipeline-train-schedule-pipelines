pipeline{
agent any
      stages{
          stage(build){
                    steps{
                        echo 'Running Build Automation worked By chandra'
                        sh './gradlew build --no-daemon'
                        archieveArtifacts  artifacts: 'dist/trainschedule.zip'                      
                         }
                      }
            }
         }
